// Copy this file to `config.js` (which is gitignored) and paste your key.
//
// You need a Google Maps API key with these APIs enabled in Google Cloud Console:
//   - Map Tiles API        (Photorealistic 3D Tiles — the 3D world)
//   - Maps JavaScript API   (loads the Places library)
//   - Places API            (location search)
//
// Restrict the key by HTTP referrer before deploying anywhere public.
window.HORSEBACK_CONFIG = {
  GOOGLE_MAPS_API_KEY: "YOUR_API_KEY_HERE",
};
